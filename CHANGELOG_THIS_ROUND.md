# 本轮实现说明

## 后端

- 将 `music_service.py` 移入 `backend/music_service.py`，统一后端目录边界。
- `backend/run_server.py` 默认支持热加载，便于前后端同时开发。
- 推荐改为后台任务：
  - 后端启动时预热一次当前推荐。
  - 提供 `/api/recommendations/current` 读取当前推荐。
  - 推荐最多生成 15 首。
  - 在线推荐为空时，自动降级到本地曲库与播放历史推荐。
- 新增 `backend/music_db.py`：
  - 用 SQLite 管理本地曲库与播放历史。
  - 本地歌曲新增、删除会同步更新数据库。
  - 播放开始即记录一次历史，并同步累加本地歌曲播放次数。
- SQLite 数据文件 `music_player.sqlite3` 作为运行时数据，不纳入 Git。
- 打包/桌面运行时，SQLite 会写入可执行文件同目录，避免写到只读 `_MEIPASS`。

## 前端

- Web Audio 已接入播放链路：
  - 10 段 EQ
  - 预设
  - 响度归一化近似
  - 限制保护
  - 音量增强
  - 简单空间感
- Web Audio 仅在用户手动“应用到当前播放”时接管，避免第三方跨源音频静音。
- 推荐页优化：
  - 上方展示 5 张横向专辑图卡片。
  - 下方保留推荐列表。
  - 进入推荐页优先读取当前推荐缓存，不重复自动生成。
- 修正歌曲表格列宽和播放按钮对齐，减少长专辑名导致的错位。

## 文档

- README 已同步当前推荐、SQLite、本地推荐兜底、播放统计与音效现状。
